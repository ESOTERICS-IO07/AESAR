robot:
  name: ARACHNID

  default_mode: MANUAL

  initial_state: DISCONNECTED

  coordinate_frames:
    base: base_link
    odometry: odom
    map: map

backend:
  host: 0.0.0.0
  port: 8000

frontend:
  websocket_path: /ws

logging:
  level: INFO